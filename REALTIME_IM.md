# 一对一实时通信

本模块基于 Netty NIO + WebSocket，保留原有 `/im` 地址以及 `100/101/102` 指令，并增加可靠送达、已读回执、离线补偿和心跳指令。当前明确不实现群聊。

## 升级步骤

1. 对已有数据库执行 `database/reliable_im_upgrade.sql`。全新环境直接导入更新后的 `database/teriteri.sql`。
2. 启动 MySQL、Redis 和后端。IM 服务继续监听 `7071`，路径为 `/im`。
3. 启动用户端。每个浏览器标签页使用独立的 `deviceId`，同一账号的所有在线连接都会收到消息同步。

迁移脚本会把升级前的历史消息标为已送达、已读，避免旧记录在首次连接时被当成离线消息重放。

## 指令

| code | 方向 | 用途 |
| --- | --- | --- |
| 100 | 客户端 -> 服务端 | JWT 鉴权并登记设备 |
| 101 | 客户端 -> 服务端 | 发送私聊，必须携带 `clientMessageId` |
| 102 | 客户端 -> 服务端 | 两分钟内撤回自己的消息 |
| 103 | 客户端 -> 服务端 | 接收设备确认送达 |
| 104 | 客户端 -> 服务端 | 当前会话确认已读 |
| 105 | 双向 | 25 秒应用层心跳；服务端 75 秒读空闲断开 |
| 106 | 客户端 -> 服务端 | 按游标继续拉取离线消息 |

服务端 `whisper` 事件兼容原有的 `接收`、`撤回`、`已读` 等类型，并新增 `送达` 与 `已读回执`。离线消息每批最多 100 条，客户端依据批次游标自动继续拉取。

## 可靠性语义

- 先写 MySQL，再向发送者和接收者的全部在线设备广播。
- `(user_id, client_message_id)` 唯一索引提供发送幂等；重连重发不会产生重复记录。
- 任一接收设备 ACK 后写入 `delivered_at`；打开聊天窗口后写入 `read_at` 并通知发送者全部设备。
- 未写入 `delivered_at` 的记录视为离线消息，鉴权成功后按 ID 顺序分批补发。
- 客户端按数据库 ID 或客户端消息 ID 双重去重，展示“发送中、已发送、已送达、已读、发送失败”。
- 每个聊天窗口在线标记按设备保存在 Redis Set 中并设置 75 秒有效期，某一设备离开不会覆盖其他设备状态。

## 验证

```powershell
mvn.cmd -DskipTests compile
mvn.cmd -Dtest=ReliableImProtocolTest test
```

前端执行：

```powershell
npm run build
```
